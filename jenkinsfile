pipeline {
    agent any
    stages {
        stage('build the code') {
            steps {
                echo "code build successfully"
            }
        }
        if(true) {
            build job: 'validate'
        }    
    }
}
